<!DOCTYPE html>
<html>

<head>
  <title><%= title %></title>
  <meta charset="UTF-8">
  <style>
    pre#data {
      background-color: #f4f4f4;
      padding: 15px;
      border: 1px solid #ddd;
      border-radius: 4px;
      overflow-x: scroll;
    }
  </style>
</head>

<body>
  <h1><%= title %></h1>
  <h3> Instructions </h3>

  <p>Click on the buttons below to fetch data from the API endpoints.</p>

  <button onclick="fetchData('/api/posts')">Get All Posts</button>
  <button onclick="fetchData('/api/users')">Get All Users</button>
  <button onclick="fetchData('/api/posts/location')">Get Posts by Location</button>
  <button onclick="fetchData('/api/post/17102022110213')">Get Post Details (Sample Post ID)</button>
  <button onclick="fetchData('/api/user/John%20Doe')">Get User Profile (Sample User)</button>

  <pre id="data">
    <!-- Data will appear here -->
  </pre>

  <script>
    function fetchData(endpoint) {
      fetch(endpoint) // Fetch from the API endpoint
        .then(response => response.json()) // Parse as JSON
        .then(data => {
          document.getElementById('data').textContent = JSON.stringify(data, null, 4);
        }) // Print the stringified JSON data to the text content of the "data" element
        .catch(error => {
          console.error("Error fetching data:", error);
        }); // Catch any errors and log them to the console 
    }
  </script>
</body>